#ifndef DRV8353_HPP
#define DRV8353_HPP

#include <stdint.h>

#include "bus_if.hpp"

class DRV8353
{
// private:
//     bus_if &bus_;
//     gpio_if &enable_;

// public:
//     DRV8353(bus_if &bus, gpio_if &enable);

//     ~DRV8353() = default;

//     int init(delay_if &delay);

//     uint16_t ReadReg(const uint8_t &addr);
};

#endif
